---
// Kannada homepage. Astro sets currentLocale = 'kn' for this /kn/ route, so the
// shared Home component renders its Kannada strings automatically.
import Home from '../../components/pages/Home.astro';
---
<Home />
